# TODO

- Fix flaky performance tests in `AppPerformance.test.tsx` and `InterviewFormPerformance.test.tsx` which struggle with async Context updates.
- Improve test coverage for Firebase repositories.
